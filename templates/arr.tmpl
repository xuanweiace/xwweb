<!-- templates/arr.tmpl -->
<html>
<body>
    <p>key1={{.key1}}</p>
    {{range $index, $ele := .stu }}
    <p>{{ $index }}: {{ $ele.Name }} is {{ $ele.Age }} years old</p>
    {{ end }}
</body>
</html>